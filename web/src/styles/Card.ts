import { makeStyles, createStyles } from "@material-ui/core/styles"
/**
 * Card Popover style
 */
export const useCardPopoverStyles = makeStyles(() =>
  createStyles({
    root: {
      minWidth: "320px",
    },
  })
)
